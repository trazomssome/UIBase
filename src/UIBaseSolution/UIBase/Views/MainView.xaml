<Window x:Class="UIBase.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIBase.Views" xmlns:controls="clr-namespace:UIBase.Controls"
        mc:Ignorable="d"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="MainView" Height="450" Width="800">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="20"/>
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "Auto"/>
            <RowDefinition/>
            <RowDefinition Height = "Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <controls:TitleBar />

        <!-- Body -->
        <StackPanel Background="Blue" Grid.Row="1"/>

        <!-- Footer -->
        <StackPanel Background="Green" Height="20" Grid.Row="2"/>
        
    </Grid>
</Window>
